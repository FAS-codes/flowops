# Base URL of the deployed backend API, including the /api suffix.
# Leave unset for local dev (Vite proxies /api to http://localhost:4000).
# Example (production): https://flowops-api.onrender.com/api
VITE_API_URL=
